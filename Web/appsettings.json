{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
            // Add more specific log levels if needed
            // "SurveyApp.DAL.SurveyRepository": "Debug"
        }
    },
    "AllowedHosts": "*",
    "ConnectionStrings": {
        // IMPORTANT: Use Integrated Security=SSPI for Windows Authentication.
        // Ensure the identity running your IIS App Pool (or Kestrel process) has DB permissions.
        // TrustServerCertificate=True may be needed for local dev/non-domain environments but check security implications.
        "SurveyDbConnection": "Server=SQL03;Database=SurveyDB;Integrated Security=SSPI;TrustServerCertificate=True;"
    }
}